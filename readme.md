It is version 3.0
To remove the existing repo origin--git remote remove origin
To add new repo URL--git remote add origin https://github.com/your-username/new-repo-name.git
To check at which repo did the code will push--git remote -v
Renames the current branch to main--git branch -M main
To push the code on git--git push -u origin main 


# to create a new repo
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/tech-project-8/sticks.git
git push -u origin main
